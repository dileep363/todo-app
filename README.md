# todo-app
First download the project into your local machine and unzip the downloaded file and then download the dependencies using the command "npm install" and then  run the app using the command "npm start"
